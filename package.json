{
  "name": "bigeo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "BiGeo: resolving Indian addresses via the Bharat Address Graph and Claude AI.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
